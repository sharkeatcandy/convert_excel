# 薪資資料轉錄工具

純前端 Excel 轉錄網頁。兩個 Excel 檔案只在瀏覽器本機處理，不會上傳。

## 使用方式

直接開啟 `index.html`，依序選擇來源資料與空白範本，按「產生轉錄檔案」。

## 欄位規則

- C → C
- S + T − AE → L
- V → M
- W + AN → N
- Z → V
- AD → S
- B 的西元年月拆分為 J（民國年）與 K（月）
